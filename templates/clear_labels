<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Clear Labels</title>
</head>
<body>
    <h1>Clear All Labels</h1>
    <p>Are you sure you want to clear all labels? This action cannot be undone.</p>
    <form method="POST">
        <button type="submit">Yes, Clear All Labels</button>
    </form>
    <a href="{{ url_for('home') }}">Cancel</a>
</body>
</html>